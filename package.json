{
  "name": "the-chronicles-of-the-few",
  "version": "1.0.0",
  "description": "Website for The Chronicles of the Few book series",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html",
    "lint": "stylelint '**/*.css' && eslint '**/*.js'",
    "format": "prettier --write '**/*.{html,css,js}'"
  },
  "author": "Dawn Clark",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.56.0",
    "parcel": "^2.11.0",
    "prettier": "^3.2.4",
    "stylelint": "^16.2.0",
    "stylelint-config-standard": "^36.0.0"
  },
  "dependencies": {
    "lightbox2": "^2.11.4"
  }
}
